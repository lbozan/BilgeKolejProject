@using System.Web.Optimization
@model BilgeKolejProject.EgitimDersleri

@{
    ViewBag.Title = "Alt Ders Düzenle";
    Layout = "~/Views/Shared/_YonetimLayout.cshtml";
}

<div class="row-fluid">
    <div class="span12">
        <!-- BEGIN VALIDATION STATES-->
        <div class="portlet box blue">
            <div class="portlet-title">
                <h4><i class="icon-reorder"></i>@ViewBag.Title</h4>
                <div class="tools">
                    <a href="javascript:;" class="collapse"></a>
                </div>
            </div>
            <div class="portlet-body form">
                @using (Html.BeginForm(null, null, FormMethod.Post, new { @class = "form-horizontal" }))
                {
                    @Html.AntiForgeryToken()
                    @Html.ValidationSummary(true)
                    @Html.HiddenFor(model => model.UstID)
                    <div class="control-group">
                        <label class="control-label">
                            @Html.LabelFor(model => model.DersAdi)
                        </label>
                        <div class="controls">
                            @Html.EditorFor(model => model.DersAdi, new { @class = "span6 m-wrap" })
                            @Html.ValidationMessageFor(model => model.DersAdi, null, new { @class = "help-inline" })
                        </div>
                    </div>
                    <div class="control-group">
                        <label class="control-label">
                            @Html.LabelFor(model => model.DersKodu)
                        </label>
                        <div class="controls">
                            @Html.EditorFor(model => model.DersKodu, new { @class = "span6 m-wrap" })
                            @Html.ValidationMessageFor(model => model.DersKodu, null, new { @class = "help-inline" })
                        </div>
                    </div>
                    <div class="form-actions">
                        <button type="submit" class="btn green">Güncelle</button>
                        <a href="@Url.Action("DersDetails", new { Id = Model.UstID })" class="btn">İptal</a>
                    </div>
                }
            </div>
        </div>
    </div>
</div>


@section Scripts {
    @Scripts.Render("~/bundles/jquery")
}
